import React from 'react';

const Loading = ({styles}) =>{
	return <div styles id="loader"><img src={'https://loading.io/spinners/cube/lg.pulsing-squares-loader.gif'}/></div>
}

export default Loading